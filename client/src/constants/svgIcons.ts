import svgTrash from 'assest/images/trash-icon.svg';
import svgCart from 'assest/images/icon-cart.svg';

import { KeyType } from 'types/keyType';

export const svgIcons: KeyType<any> = {
   trash: svgTrash,
   cart: svgCart,
};