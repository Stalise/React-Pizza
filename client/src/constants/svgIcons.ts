import svgTrash from 'assets/images/trash-icon.svg';
import svgCart from 'assets/images/icon-cart.svg';

import { KeyType } from 'types/keyType';

export const svgIcons: KeyType<any> = {
   trash: svgTrash,
   cart: svgCart,
};